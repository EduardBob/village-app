$( document ).ready(function() {

  // height of the wrapper
  $('.wrapper').height($(window).height());
  // console.log($('.main-container').innerHeight());
  // console.log($(window).height());
  // // if ($('.main-container').innerHeight() < $(window).height()) {
  // // 	$('.main-container').innerHeight($(window).height())
  // // }

  // $('body').on('touchmove', function(e) {
  //     e.preventDefault();
  // });

});